/**
 * The items this mod registers directly: the weapon item, the five attachment items and the six
 * cartridge items (`FA-3`). Not the bullet entity (`FA-6`) and not any block — this mod adds none
 * (`docs/spec/04-architecture.md`).
 */
package firearms.item;
