/**
 * The seven {@code DataComponentType} registrations and their codecs (`FA-3`,
 * `docs/spec/04-architecture.md` `ARCH-DEC-005`, `docs/spec/contracts/data-contract.md`). Not part
 * of the pure {@code firearms.model} package FA-2 landed — these types exist to persist and
 * network-synchronise {@code firearms.model} values on real item stacks.
 */
package firearms.component;
