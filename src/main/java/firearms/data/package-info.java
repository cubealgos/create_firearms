/**
 * The data loaders turning {@code data/firearms/weapon/*.json} and {@code
 * data/firearms/attachment/*.json} into {@code firearms.model} values (`FA-3`,
 * `docs/spec/domains/weapon.md` `WEAPON-DEC-003`), plus the codecs and the two lookup registries
 * that back them. Not part of the pure {@code firearms.model} package FA-2 landed.
 */
package firearms.data;
