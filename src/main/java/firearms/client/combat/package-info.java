/**
 * The bullet's client-only tracer renderer ({@code firearms.combat.BulletEntity}, registered as
 * {@code firearms:bullet}). Cosmetic only, carries no authority over any hit, damage or ammo
 * state (COMBAT-REQ-011, {@code docs/spec/domains/combat.md}). The three scope mixins live
 * outside this package (they are not yet built at FA-5).
 */
package firearms.client.combat;
