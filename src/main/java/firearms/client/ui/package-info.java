/**
 * The weapon, attachment and cartridge tooltips ({@code WeaponTooltip}, `docs/spec/domains/ui.md`
 * `UI-REQ-001`): every number formatted through {@code StatsText}'s {@code Locale.ROOT} helpers,
 * mirroring {@code create_metered_motor}'s own {@code StatsText}/{@code MeteredMotorTooltip} split.
 * Not the scope overlay or its mixins ({@code firearms.client.scope}, `FA-10`).
 */
package firearms.client.ui;
